<%= form_for(@vender) do |f| %>
  <% if @vender.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@vender.errors.count, "error") %> prohibited this vender from being saved:</h2>

      <ul>
      <% @vender.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
